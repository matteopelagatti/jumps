% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{solve_jump_spline_fast}
\alias{solve_jump_spline_fast}
\title{Alternating time-warping algorithm for smoothing splines with jumps}
\usage{
solve_jump_spline_fast(
  x_in,
  y_in,
  lambda,
  M,
  max_iter = 100L,
  tol = 1e-04,
  learning_rate = 0.1,
  gamma_init = numeric(0)
)
}
\arguments{
\item{x_in}{vector of observations for the x variable}

\item{y_in}{vector of observations for the y variable}

\item{lambda}{positive smoothing constant}

\item{M}{positive value for the maximum sum of the time extensions}

\item{max_iter}{positive integer with the maximum number of iterations}

\item{tol}{small positive number with tolerance for numerical convergence}

\item{learning_rate}{positive number: initial and fallback step size for the
Barzilai-Borwein adaptive scheme (default 0.1).}

\item{gamma_init}{optional numeric vector of length n-1 with a warm-start
value for gamma; ignored (zero initialisation used) when its length differs
from n-1.}
}
\value{
A list with the following slots:
\describe{
  \item{f}{The smoothing spline with jumps computed at x.}
  \item{gamma}{The vector of added times (time jumps).}
  \item{df}{Derivative of f.}
  \item{edf}{Number of effective degrees of freedom.}
  \item{rss}{Residual sum of squares with residuals = y - f.}
  \item{gcv}{Generalized cross-validation.}
  \item{loglik}{Gaussian log-likelihood evaluated at the unbiased residual variance sigma^2 = rss/(n - edf).}
  \item{aic}{Akaike information criterion.}
  \item{aicc}{Akaike information criterion with bias correction.}
  \item{bic}{Bayesian information criterion.}
  \item{hq}{Hannan-Quinn information criterion.}
}
}
\description{
Alternating time-warping algorithm for smoothing splines with jumps
}
