% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssj_awt.R
\name{ssj_atw}
\alias{ssj_atw}
\title{Smoothing splines with with automatic jumps detection and
fixed smoothing parameter using the Alternating time-warping algorithm}
\usage{
ssj_atw(
  y,
  x,
  lambda,
  maxsum = sd(y, na.rm = TRUE)/mean(diff(x)),
  max_iter = 100,
  tol = 1e-04,
  learning_rate = 0.1,
  gamma_init = numeric(0)
)
}
\arguments{
\item{y}{vector with the y (missing values allowed).}

\item{x}{vector with the x (no missing values allowed).}

\item{lambda}{a numeric scalar with the smoothing parameter.}

\item{maxsum}{maximum sum of additional standard deviations.}

\item{max_iter}{maximum number of iterations (default 100).}

\item{tol}{tolerance for convergence (default 1e-4).}

\item{learning_rate}{initial and fallback step size for the Barzilai-Borwein adaptive scheme (default 0.1).}

\item{gamma_init}{optional numeric vector of length n-1 with a warm-start value for gamma; ignored when its length does not match n-1.}
}
\value{
list with the following slots:
\itemize{
 \item nobs: number of observations.
 \item df: model's degrees of freedom.
 \item maxsum: maximum sum of additional standard deviations.
 \item loglik: Gaussian log-likelihood evaluated at the unbiased residual variance sigma^2 = rss/(n - edf).
 \item gamma: additional times.
 \item ic: vector of information criteria (aic, aicc, bic, hq).
 \item smoothed_level: vector with smoothing splines with jumps.
 \item x: the ordered x variable
}
}
\description{
Similar to ssj_fix, but using the Alternating time-warping algorithm.
}
\examples{
plot(faithful$eruptions, faithful$waiting)
of_jum <- ssj_atw(y = faithful$waiting,
                  x = faithful$eruptions,
                  lambda = 1,
                  maxsum = 150)
lines(x = sort(faithful$eruptions),
      y = of_jum$smoothed_level, col = "red", lwd = 2)
      
}
